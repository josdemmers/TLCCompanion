<Page
    x:Class="TLCCompanion.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:TLCCompanion.Helpers"
    xmlns:local="clr-namespace:TLCCompanion.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingsPage"
    d:DataContext="{d:DesignInstance local:SettingsPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>

    <StackPanel Width="Auto">
        <TextBlock
            FontSize="20"
            FontWeight="Medium"
            Text="General" />
        <TextBlock Margin="0,12,0,0" Text="Dll location" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <RadioButton
                Grid.Column="0"
                Margin="0,12,0,0"
                Command="{Binding ViewModel.ChangeDllCommand, Mode=OneWay}"
                CommandParameter="Default"
                Content="Default"
                GroupName="dllLocation"
                IsChecked="{Binding ViewModel.UseDefaultDllLocation, Mode=OneWay}" />
            <RadioButton
                Grid.Column="1"
                Margin="0,8,0,0"
                Command="{Binding ViewModel.ChangeDllCommand, Mode=OneWay}"
                CommandParameter="Custom"
                Content="Custom"
                GroupName="dllLocation"
                IsChecked="{Binding ViewModel.UseCustomDllLocation, Mode=OneWay}" />
            <TextBox Grid.Column="2" x:Name="DllPathTextBox" Margin="0,8,1,0" Text="{Binding ViewModel.DllPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="11" IsReadOnly="True" VerticalAlignment="Center" Focusable="False"/>
            <Button Grid.Column="3" Margin="0,8,0,0" Content="..." Click="ButtonDllPath_Click" Width="Auto" MinWidth="20" VerticalAlignment="Center"></Button>
        </Grid>
        
        <TextBlock
            FontSize="20"
            FontWeight="Medium"
            Text="Personalization" />
        <TextBlock Margin="0,12,0,0" Text="Theme" />
        <RadioButton
            Margin="0,12,0,0"
            Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
            CommandParameter="theme_light"
            Content="Light"
            GroupName="themeSelect"
            IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}" />
        <RadioButton
            Margin="0,8,0,0"
            Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
            CommandParameter="theme_dark"
            Content="Dark"
            GroupName="themeSelect"
            IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}" />

        <TextBlock
            Margin="0,24,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="About" />
        <TextBlock Margin="0,12,0,0" Text="{Binding ViewModel.AppVersion, Mode=OneWay}" />
    </StackPanel>
</Page>
