<Window x:Class="TLCCompanion.Parser.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TLCCompanion.Parser"
        xmlns:viewmodels="clr-namespace:TLCCompanion.Parser.ViewModels" 
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:MainWindowViewModel/>
    </Window.DataContext>
    <Grid VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Game data: "/>
        <TextBox x:Name="GameDataLocationPathTextBox" Grid.Row="0" Grid.Column="1" Text="{Binding GameDataLocationPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="Auto" Height="25" FontSize="11" VerticalContentAlignment="Center" IsReadOnly="True" />
        <Button Grid.Row="0" Grid.Column="2" Content="..." Click="ButtonBase_OnClick" Width="Auto" MinWidth="20" Margin="3 0 0 0"></Button>
        <Button Grid.Row="1" Content="POI worldmap" Command="{Binding ParsePOIWorldmapCommand}" Width="Auto" MinWidth="20" Margin="3 0 0 0"></Button>
    </Grid>
</Window>
